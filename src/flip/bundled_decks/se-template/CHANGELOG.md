# Changelog — 软件工程模板

## 1.1

- ch5 / q-76c99e22d260
  - 题目 `Which is not one of the key questions ... daily Scrum meeting?`
  - 修改 `user_note`，改为说明 C 之所以错误，是因为它要求追问问题成因，不属于 daily Scrum 的标准三问。
- ch6 / q-d78227832efd
  - 题目 `Which of the following can contribute to team toxicity?`
  - 答案由 `ABD` 改为 `ACD`
  - 修改 `user_note`，改为按课程 PPT 将 `poorly coordinated procedures` 计入。
- ch8 / q-b2ceca5184aa
  - 题目 `User stories are complete descriptions ...`
  - 修改 `user_note`，改为按当前课程题库语境保持答案 `A`。
- ch8 / q-abb82d0cfebb
  - 题目 `In requirements validation ... technical feasibility`
  - 答案由 `B` 改为 `A`
  - 修改 `user_note`，改为说明技术可行性属于 validation 时需要复核的内容。
- ch21 / q-65b1160a7f92
  - 题干由 `defines three core steps` 改为 `defines five core steps. Which of the following are the first three?`
  - 修改 `user_note`，改为按 DMAIC 说明前 3 步是 define、measure、analyze。
- ch24 / q-0552df335186
  - 题目 `Test case design for OO software ...`
  - 答案由 `A` 改为 `B`
  - 修改 `user_note`，改为强调 OO 测试的最小可测试单元是封装类，而不是单个 operation。
- ch27 / q-4380483690dd
  - 题目 `Security correctness checks should be included ...`
  - 答案由 `ABC` 改为 `ACD`
  - 修改 `user_note`，改为按课程材料把 testing 纳入 security correctness checks。
- ch29 / q-aeb972fa984c
  - 题目 `Which of these are valid software configuration items?`
  - 答案由 `ABCD` 改为 `BCD`
  - 修改 `user_note`，改为按课程 PPT 将 tools 视为 SCM element，而非 SCI 本体。
- ch29 / q-5002a578ae59
  - 题目 `Configuration issues ... Web and Mobile Apps include:`
  - 答案由 `ABC` 改为 `ACD`
  - 修改 `user_note`，改为按课程 PPT 主项分类修正为 Content、People、Politics。
- ch30 / q-7879e356a5fa
  - 题目 `The depth of inheritance tree (DIT) metric ...`
  - 修改 `user_note`，补充课程材料之间存在分歧，当前 deck 仍保持答案 `C`。
- ch31 / q-7e5ab11dfb72
  - 题目 `The major areas of problem decomposition ...`
  - 答案由 `BC` 改为 `B`
  - 修改 `user_note`，改为说明 problem decomposition 更直接对应 functionality to be delivered。
- ch36 / q-cbf483bcdeb9
  - 题目 `Business process reengineering does not have a start or end ...`
  - 答案由 `A` 改为 `B`
  - 修改 `user_note`，改为按课程材料列出的阶段说明 BPR 不是无边界的演化过程。
- ch36 / q-530a343f9456
  - 题目 `The first reverse engineering activity involves seeking to understand`
  - 答案由 `B` 改为 `A`
- ch36 / q-a57a49fc05ad
  - 题目 `Code restructuring is a good example of software reengineering.`
  - 答案由 `B` 改为 `A`
  - 修改 `user_note`，改为按课程材料说明 code restructuring 本身就是 software reengineering 的典型活动。

## 1

初始版本,561 题。
